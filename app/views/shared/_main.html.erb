<%= render Developers::BannerComponent.new([
      Developers::UnseededWarningComponent.new(seedable: Rails.env.development?),
      Developers::InvisibleBannerComponent.new(current_user),
      Developers::NewFieldsComponent.new(current_user)
    ]) %>

<main class="mx-auto">
  <%= yield %>
</main>
